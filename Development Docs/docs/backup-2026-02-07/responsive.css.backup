/* ===================================
   CDS Website - Responsive Design
   File: responsive.css
   Purpose: Media queries and responsive styles
   RESTORED: Original responsive code + iOS enhancements
   =================================== */

/* ===================================
   ORIGINAL RESPONSIVE CODE (from concept1)
   UPDATED: Breakpoint changed from 968px to 1100px for iPad Pro support
   =================================== */

@media (max-width: 1100px) {
    .nav-menu {
        position: fixed;
        top: 80px;
        left: -100%;
        width: 100%;
        height: calc(100vh - 80px);
        background: var(--white);
        flex-direction: column;
        align-items: flex-start;
        padding: var(--spacing-xl);
        transition: left var(--transition-base);
        box-shadow: var(--shadow-xl);
    }

    .nav-menu.active {
        left: 0;
    }

    .mobile-toggle {
        display: flex;
    }

    .mobile-toggle.active span:nth-child(1) {
        transform: rotate(45deg) translate(5px, 5px);
    }

    .mobile-toggle.active span:nth-child(2) {
        opacity: 0;
    }

    .mobile-toggle.active span:nth-child(3) {
        transform: rotate(-45deg) translate(7px, -6px);
    }

    .hero-stats {
        grid-template-columns: 1fr;
    }

    .hero-cta {
        flex-direction: column;
    }

    .focus-grid,
    .why-grid,
    .industries-grid,
    .services-grid {
        grid-template-columns: 1fr;
    }

    .footer-top {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 640px) {
    :root {
        --spacing-3xl: 3rem;
        --spacing-2xl: 2rem;
    }

    .logo-text {
        display: none;
    }

    .hero {
        min-height: auto;
        padding: var(--spacing-3xl) 0;
    }

    .testimonial-wrapper {
        padding: var(--spacing-lg);
    }

    .global-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* ===================================
   AOS Animations
   =================================== */

[data-aos] {
    opacity: 0;
    transition-property: opacity, transform;
}

[data-aos].aos-animate {
    opacity: 1;
}

[data-aos="fade-up"] {
    transform: translateY(30px);
}

[data-aos="fade-up"].aos-animate {
    transform: translateY(0);
}

[data-aos="fade-right"] {
    transform: translateX(-30px);
}

[data-aos="fade-right"].aos-animate {
    transform: translateX(0);
}

[data-aos="zoom-in"] {
    transform: scale(0.9);
}

[data-aos="zoom-in"].aos-animate {
    transform: scale(1);
}

[data-aos="flip-left"] {
    transform: perspective(2500px) rotateY(-100deg);
}

[data-aos="flip-left"].aos-animate {
    transform: perspective(2500px) rotateY(0);
}

/* ===================================
   Page Hero (for inner pages)
   =================================== */

.page-hero {
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    padding-top: 120px;
    padding-bottom: var(--spacing-3xl);
}

.page-hero-content {
    text-align: center;
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

.page-hero-content h1 {
    color: var(--white);
    margin-bottom: var(--spacing-md);
}

.page-hero-content p {
    color: var(--gray-300);
    font-size: 1.125rem;
    margin-bottom: var(--spacing-xl);
}

/* ===================================
   Services Detail Page
   =================================== */

.services-detail-section {
    padding: var(--spacing-3xl) 0;
}

.service-detail {
    margin-bottom: var(--spacing-3xl);
    padding: var(--spacing-2xl);
    background: var(--white);
    border-radius: var(--radius-xl);
    border: 2px solid var(--gray-200);
    transition: all var(--transition-base);
}

.service-detail:hover {
    border-color: var(--primary-400);
    box-shadow: var(--shadow-xl);
}

.service-detail-header {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-lg);
    margin-bottom: var(--spacing-xl);
}

.service-detail-icon {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    color: var(--white);
    flex-shrink: 0;
}

.service-detail-header h2 {
    margin-bottom: 0.5rem;
}

.service-tagline {
    font-size: 1.125rem;
    color: var(--gray-500);
    margin: 0;
}

.service-detail-content>p {
    font-size: 1.125rem;
    margin-bottom: var(--spacing-xl);
}

.service-offerings h3 {
    font-size: 1.5rem;
    margin-bottom: var(--spacing-lg);
    color: var(--dark-900);
}

.offerings-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-lg);
}

.offering-item {
    padding: var(--spacing-lg);
    background: var(--gray-100);
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
}

.offering-item:hover {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(217, 70, 239, 0.1));
    transform: translateY(-5px);
}

.offering-item i {
    font-size: 2rem;
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--spacing-sm);
}

.offering-item h4 {
    font-size: 1.125rem;
    margin-bottom: var(--spacing-sm);
    color: var(--dark-900);
}

.offering-item p {
    font-size: 0.9375rem;
    color: var(--gray-500);
    margin: 0;
}

/* ===================================
   Operating Model Timeline
   =================================== */

.operating-model-section {
    background: var(--gray-100);
}

.model-timeline {
    max-width: 900px;
    margin: 0 auto;
    position: relative;
}

.model-timeline::before {
    content: '';
    position: absolute;
    left: 40px;
    top: 0;
    bottom: 0;
    width: 4px;
    background: linear-gradient(180deg, var(--primary-600), var(--accent-600));
}

.model-step {
    display: flex;
    gap: var(--spacing-xl);
    margin-bottom: var(--spacing-2xl);
    position: relative;
}

.step-number {
    width: 80px;
    height: 80px;
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--white);
    flex-shrink: 0;
    box-shadow: 0 4px 20px rgba(139, 92, 246, 0.4);
    position: relative;
    z-index: 1;
}

.step-content {
    flex: 1;
    background: var(--white);
    padding: var(--spacing-xl);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
}

.step-content h3 {
    font-size: 1.5rem;
    margin-bottom: var(--spacing-sm);
    color: var(--dark-900);
}

.step-content>p {
    margin-bottom: var(--spacing-md);
}

.step-list {
    padding-left: var(--spacing-md);
}

.step-list li {
    position: relative;
    padding-left: var(--spacing-md);
    margin-bottom: var(--spacing-sm);
    color: var(--gray-500);
}

.step-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--primary-600);
    font-weight: 700;
}

/* ===================================
   Case Studies Page
   =================================== */

.case-studies-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: var(--spacing-xl);
}

.case-study-card {
    background: var(--white);
    border-radius: var(--radius-xl);
    overflow: hidden;
    border: 2px solid var(--gray-200);
    transition: all var(--transition-base);
}

.case-study-card:hover {
    transform: translateY(-10px);
    border-color: var(--primary-400);
    box-shadow: 0 20px 40px rgba(139, 92, 246, 0.2);
}

.case-study-header {
    padding: var(--spacing-xl);
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    color: var(--white);
}

.case-study-category {
    display: inline-block;
    padding: 0.375rem 0.875rem;
    background: rgba(255, 255, 255, 0.2);
    backdrop-filter: blur(10px);
    border-radius: var(--radius-full);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: var(--spacing-sm);
}

.case-study-header h3 {
    color: var(--white);
    font-size: 1.5rem;
    margin-bottom: 0;
}

.case-study-body {
    padding: var(--spacing-xl);
}

.case-study-section {
    margin-bottom: var(--spacing-lg);
}

.case-study-section:last-child {
    margin-bottom: 0;
}

.case-study-section h4 {
    font-size: 1rem;
    color: var(--primary-600);
    margin-bottom: var(--spacing-sm);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

.case-study-section p {
    font-size: 0.9375rem;
    margin: 0;
}

.case-study-impact {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
    gap: var(--spacing-md);
    padding: var(--spacing-lg);
    background: var(--gray-100);
    border-radius: var(--radius-md);
}

.impact-metric {
    text-align: center;
}

.impact-value {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.25rem;
}

.impact-label {
    font-size: 0.75rem;
    color: var(--gray-500);
    text-transform: uppercase;
    letter-spacing: 0.05em;
}

/* ===================================
   Impact Page
   =================================== */

.impact-showcase {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-xl);
}

.impact-card {
    background: var(--white);
    padding: var(--spacing-xl);
    border-radius: var(--radius-xl);
    border: 2px solid var(--gray-200);
    transition: all var(--transition-base);
}

.impact-card:hover {
    transform: translateY(-8px);
    border-color: var(--primary-400);
    box-shadow: var(--shadow-xl);
}

.impact-card h3 {
    font-size: 1.25rem;
    margin-bottom: var(--spacing-sm);
    color: var(--dark-900);
}

.impact-card .highlight {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--primary-600);
    margin-bottom: var(--spacing-sm);
}

.impact-card p {
    font-size: 0.9375rem;
    margin: 0;
}

/* ===================================
   Technology Stack Section
   =================================== */

.tech-stack-section {
    background: var(--dark-900);
    color: var(--white);
}

.tech-stack-section .section-label {
    background: rgba(139, 92, 246, 0.2);
    color: var(--primary-400);
}

.tech-stack-section .section-title {
    color: var(--white);
}

.tech-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: var(--spacing-lg);
}

.tech-item {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    padding: var(--spacing-lg);
    border-radius: var(--radius-lg);
    border: 1px solid rgba(255, 255, 255, 0.1);
    text-align: center;
    transition: all var(--transition-base);
}

.tech-item:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: var(--primary-400);
    transform: translateY(-5px);
}

.tech-item i {
    font-size: 3rem;
    background: linear-gradient(135deg, var(--primary-400), var(--accent-400));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: var(--spacing-sm);
}

.tech-item h4 {
    color: var(--white);
    font-size: 1rem;
}

/* ===================================
   Company Page
   =================================== */

.company-intro {
    max-width: 900px;
    margin: 0 auto var(--spacing-3xl);
    text-align: center;
}

.company-intro h2 {
    margin-bottom: var(--spacing-md);
}

.company-intro p {
    font-size: 1.125rem;
    margin-bottom: var(--spacing-md);
}



/* ===================================
   Responsive Adjustments for New Pages
   =================================== */

@media (max-width: 968px) {
    .page-hero {
        min-height: 50vh;
        padding-top: 100px;
    }

    .service-detail-header {
        flex-direction: column;
    }

    .model-timeline::before {
        left: 20px;
    }

    .step-number {
        width: 60px;
        height: 60px;
        font-size: 1.25rem;
    }

    .case-studies-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 640px) {
    .service-detail {
        padding: var(--spacing-lg);
    }

    .offerings-grid {
        grid-template-columns: 1fr;
    }

    .model-step {
        gap: var(--spacing-md);
    }

    .tech-grid {
        grid-template-columns: repeat(2, 1fr);
    }
}

/* ===================================
   Services Carousel & Flip Cards
   =================================== */

.services-carousel-wrapper {
    position: relative;
    padding: 0 60px;
    margin: var(--spacing-2xl) 0;
}

.services-carousel {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--spacing-xl);
    transition: all 0.5s ease;
}

.service-flip-card {
    perspective: 1000px;
    height: 400px;
}

.flip-card-inner {
    position: relative;
    width: 100%;
    height: 100%;
    text-align: center;
    transition: transform 0.8s;
    transform-style: preserve-3d;
}

.service-flip-card:hover .flip-card-inner {
    transform: rotateY(180deg);
}

.flip-card-front,
.flip-card-back {
    position: absolute;
    width: 100%;
    height: 100%;
    backface-visibility: hidden;
    -webkit-backface-visibility: hidden;
    border-radius: var(--radius-xl);
    padding: var(--spacing-2xl);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

.flip-card-front {
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(217, 70, 239, 0.1));
    border: 2px solid var(--gray-200);
}

.flip-card-back {
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    color: var(--white);
    transform: rotateY(180deg);
}

.service-icon-large {
    width: 100px;
    height: 100px;
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    color: var(--white);
    margin-bottom: var(--spacing-lg);
    box-shadow: 0 10px 30px rgba(139, 92, 246, 0.3);
}

.flip-card-front h3 {
    font-size: 1.5rem;
    margin-bottom: var(--spacing-sm);
    color: var(--dark-900);
}

.flip-card-front .service-tagline {
    font-size: 1rem;
    color: var(--gray-500);
    margin-bottom: var(--spacing-lg);
}

.flip-hint {
    display: flex;
    align-items: center;
    gap: var(--spacing-xs);
    color: var(--primary-600);
    font-size: 0.875rem;
    margin-top: auto;
}

.flip-hint i {
    animation: rotate 2s linear infinite;
}

@keyframes rotate {
    from {
        transform: rotate(0deg);
    }

    to {
        transform: rotate(360deg);
    }
}

.flip-card-back h3 {
    color: var(--white);
    font-size: 1.5rem;
    margin-bottom: var(--spacing-lg);
}

.service-list-compact {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--spacing-xl) 0;
    text-align: left;
    width: 100%;
}

.service-list-compact li {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    margin-bottom: var(--spacing-sm);
    color: var(--white);
    font-size: 0.9375rem;
}

.service-list-compact li i {
    color: var(--cyan-300);
    flex-shrink: 0;
}

.carousel-nav {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 50px;
    height: 50px;
    background: var(--white);
    border: 2px solid var(--gray-200);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all var(--transition-base);
    z-index: 10;
}

.carousel-nav:hover {
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    color: var(--white);
    border-color: var(--primary-600);
    transform: translateY(-50%) scale(1.1);
}

.carousel-nav.prev {
    left: 0;
}

.carousel-nav.next {
    right: 0;
}

.carousel-dots {
    display: flex;
    justify-content: center;
    gap: var(--spacing-sm);
    margin-top: var(--spacing-xl);
}

.carousel-dot {
    width: 12px;
    height: 12px;
    background: var(--gray-300);
    border-radius: 50%;
    cursor: pointer;
    transition: all var(--transition-base);
}

.carousel-dot.active {
    background: linear-gradient(135deg, var(--primary-600), var(--accent-600));
    transform: scale(1.2);
}

@media (max-width: 968px) {
    .services-carousel {
        grid-template-columns: 1fr;
    }

    .services-carousel-wrapper {
        padding: 0;
    }

    .carousel-nav {
        display: none;
    }

    .team-grid,
    .culture-grid {
        grid-template-columns: 1fr;
    }
}

@media (max-width: 640px) {
    .service-flip-card {
        height: 350px;
    }

    .flip-card-front,
    .flip-card-back {
        padding: var(--spacing-lg);
    }

    .service-icon-large {
        width: 80px;
        height: 80px;
        font-size: 2.5rem;
    }
}

/* Service Detail Cards - Compact Layout */
.service-detail {
    max-width: 1000px;
    margin-left: auto;
    margin-right: auto;
}

.offerings-grid {
    grid-template-columns: repeat(2, 1fr) !important;
}

/* ===================================
   iOS-SPECIFIC ENHANCEMENTS
   =================================== */

/* iOS Safari - Fix viewport height issues */
@supports (-webkit-touch-callout: none) {
    .hero {
        min-height: -webkit-fill-available;
    }

    .nav-menu {
        height: -webkit-fill-available;
    }
}

/* iOS - Prevent zoom on input focus */
@media (max-width: 767px) {

    input[type="text"],
    input[type="email"],
    input[type="tel"],
    textarea,
    select {
        font-size: 16px;
        /* Prevents iOS zoom */
    }
}

/* iOS - Smooth momentum scrolling */
.nav-menu,
.modal,
.dropdown {
    -webkit-overflow-scrolling: touch;
}

/* iOS - Custom tap highlight */
html {
    -webkit-tap-highlight-color: rgba(139, 92, 246, 0.2);
}

a,
button,
.btn {
    -webkit-tap-highlight-color: rgba(139, 92, 246, 0.2);
}

/* Mobile flip card support - enable click/tap */
.service-flip-card.flipped .flip-card-inner {
    transform: rotateY(180deg);
}

/* ===================================
   MOBILE-FIRST BREAKPOINT STRUCTURE
   Blueprint-compliant responsive design
   Added: Phase 1 - Breakpoint Structure
   =================================== */

/* ===================================
   BASE STYLES (Mobile First - 320px+)
   Default styles for all devices
   =================================== */

/* Base mobile styles are defined in other CSS files */
/* This section will be populated in Phase 2-5 */

/* ===================================
   SMALL MOBILE (480px+)
   Large phones in portrait
   iPhone 6/7/8 Plus, Galaxy S series
   =================================== */

@media (min-width: 480px) {
    /* Phase 3: Hero font scaling */
    /* Phase 4: Grid adjustments */
}

/* ===================================
   TABLET (768px+)
   Tablets in portrait, small laptops
   iPad, Android tablets
   =================================== */

@media (min-width: 768px) {
    /* Phase 2: Navigation optimization */
    /* Phase 3: Hero 2-column stats */
    /* Phase 4: Cards 2-column layout */
}

/* ===================================
   DESKTOP (1024px+)
   Desktop, iPad Pro landscape
   Standard desktop screens
   =================================== */

@media (min-width: 1024px) {
    /* Phase 3: Hero 4-column stats */
    /* Phase 4: Cards 3-column layout */
}

/* ===================================
   LARGE DESKTOP (1280px+)
   Large desktop screens
   Full HD displays
   =================================== */

@media (min-width: 1280px) {
    /* Phase 3: Maximum hero font size */
    /* Phase 4: 4-column grids for some sections */

    /* Show scroll indicator on desktop only */
    .scroll-indicator {
        display: block;
    }
}

/* ===================================
   2K/4K DISPLAYS (1536px+)
   High-resolution displays
   2K, 4K, and ultra-wide monitors
   =================================== */

@media (min-width: 1536px) {
    /* Phase 3: Enhanced spacing */
    /* Container max-width adjustments */
}

/* ===================================
   PHASE 1 COMPLETE
   Breakpoint structure added
   Ready for Phase 2: Navigation fixes
   =================================== */